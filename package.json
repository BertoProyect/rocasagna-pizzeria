{
  "name": "rocasagna-pizzeria",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build && vite build --ssr src/entry-server.tsx --outDir dist-ssr && node scripts/prerender.mjs",
    "build:demo": "tsc -b && vite build --mode demo && vite build --mode demo --ssr src/entry-server.tsx --outDir dist-ssr && node scripts/prerender.mjs",
    "preview": "vite preview",
    "lint": "oxlint",
    "build:orbita": "tsc -b && vite build --mode orbita && vite build --mode orbita --ssr src/entry-server.tsx --outDir dist-ssr && node scripts/prerender.mjs"
  },
  "dependencies": {
    "@fontsource-variable/jost": "^5.3.0",
    "@phosphor-icons/react": "^2.1.10",
    "gsap": "^3.15.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@types/node": "^24.13.3",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.7",
    "@vitejs/plugin-react": "^6.1.1",
    "oxlint": "^1.81.0",
    "tailwindcss": "^4.3.3",
    "typescript": "~6.0.2",
    "vite": "^8.3.0"
  }
}
